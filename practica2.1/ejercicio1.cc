#include <sys/types.h>
#include <sys/socket.h>
#include <netdb.h>
#include <string>
#include <iostream>

int main(int argc, char** argv)
{
    int a = 0;
    std::cout << a << std::endl;

    std::cin >> a;
    std::cout << a << std::endl;

    return 0;
}